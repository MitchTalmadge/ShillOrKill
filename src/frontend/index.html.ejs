<!DOCTYPE html>
<html>
<head>
    <title>Shill or Kill</title>
    <meta charset="UTF-8"/>
    <meta name="viewport"
          content="width=device-width, initial-scale=1"/>

    <base href="/"/>

    <!-- Favicon -->
    <link rel="apple-touch-icon" sizes="180x180" href="<%= require('./resources/favicons/apple-touch-icon.png') %>">
    <link rel="manifest" href="<%= require('./resources/favicons/manifest.json') %>">
    <link rel="mask-icon" href="<%= require('./resources/favicons/safari-pinned-tab.svg') %>" color="#5bbad5">
    <link rel="icon" type="image/png" sizes="32x32" href="<%= require('./resources/favicons/favicon-32x32.png') %>">
    <link rel="icon" type="image/png" sizes="16x16" href="<%= require('./resources/favicons/favicon-16x16.png') %>">
    <meta name="msapplication-config" content="<%= require('./resources/favicons/browserconfig.xml') %>">
    <meta name="theme-color" content="#ffffff">

    <!--suppress CommaExpressionJS -->
    <script type="text/javascript">
        (function (i, s, o, g, r, a, m) {
            i['GoogleAnalyticsObject'] = r;
            i[r] = i[r] || function () {
                (i[r].q = i[r].q || []).push(arguments)
            }, i[r].l = 1 * new Date();
            a = s.createElement(o),
                m = s.getElementsByTagName(o)[0];
            a.async = 1;
            a.src = g;
            m.parentNode.insertBefore(a, m)
        })(window, document, 'script', 'https://www.google-analytics.com/analytics.js', 'ga');

        ga('create', 'UA-65554512-4', {'cookieDomain': 'none', 'siteSpeedSampleRate': 100});
    </script>

    <style type="text/css">
        body {
            background-color: #a7cde2;
        }
    </style>

    <style type="text/css">
        <%= require("!to-string-loader!css-loader!./resources/splashscreen/splashscreen.css") %>
    </style>

</head>
<body>
<sk-app></sk-app>

<div id="splashscreen">
    <div class="frost-bar">
        <div class="logo-container">
            <img class="logo" src="<%= require('./resources/images/logo.svg') %>"/>
        </div>
    </div>
</div>
</body>
</html>
